package kotlinlab.basic

/**
 * If statements are used to check on a condition for example when it's not raining it might be windy, calm or sunny
 * so we can say if sunny it's not raining and more
 */

fun main(){
    val isRaining = true
    if (isRaining) { println("It is raining heavily outside") }
}